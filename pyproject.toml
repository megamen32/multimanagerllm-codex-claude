[project]
name = "multimanager"
version = "2.0"
requires-python = ">=3.10"
dependencies = ["pystray", "Pillow"]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
